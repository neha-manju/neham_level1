//WAP to find the sum of two fractions.

#include<stdio.h>
struct sum
{
int x,y;
};
int main()
{
int z;
struct sum s;
printf("enter two numbers");
scanf("%d%d",&s.x,&s.y);
z=s.x+s.y;
printf("sum of two numbers is %d",z);
}
